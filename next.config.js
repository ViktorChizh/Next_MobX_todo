/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/Next_MobX_todo",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
};

module.exports = nextConfig
